import React from "react";
// import "./style.css";

function Card({children, props}) {
    return (
        <div class="card">
            <div class="card-body">
            </div>
        </div>
    );
}

export default Card;